# Dev-only — used by docker-compose (MySQL in Docker)
# For production use a real secret store and a managed DB.

MYSQL_ROOT_PASSWORD=rootdevonly
MYSQL_DATABASE=oxlev
MYSQL_USER=oxlev
MYSQL_PASSWORD=oxlev_dev_password
